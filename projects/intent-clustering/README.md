# Intent Clustering (Code)

This folder contains the Python code for the Intent Clustering project.

## Run
1) Install packages:
   pip install -r requirements.txt

2) Run:
   python ticket_intent_clustering.py

## Input
The script expects `simulated_service_tickets.xlsx` in your local machine.
